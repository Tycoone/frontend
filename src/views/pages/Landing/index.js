import React from 'react';
import {style} from './index.scss';

const Landing = ({}) => {

    return (
        <div className={style}>
            
        </div>
    );
};

Landing.propTypes = {

};

export default Landing;
