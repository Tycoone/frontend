import React from 'react';
import {style} from './index.scss';

const Profile = ({}) => {

    return (
        <div className={style}>
            
        </div>
    );
};

Profile.propTypes = {

};

export default Profile;
