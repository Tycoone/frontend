import React from 'react';
import {style} from './index.scss';

const Error = ({}) => {

    return (
        <div className={style}>
            
        </div>
    );
};

Error.propTypes = {

};

export default Error;
